#include "../headers/Note.h"
#include <string>

void Note::setTitle(const std::string &title) { this->title = title; }

void Note::setDescription(const std::string &description) {
  this->description = description;
}